import { defineConfig } from 'astro/config';

export default defineConfig({
  // vercel.app until nilaytoshniwal.com is bought; switch this line and redeploy when it is
  site: 'https://nilaytoshniwal.vercel.app',
});
